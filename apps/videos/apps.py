# ============================================================================
# Videos App Configuration
# ============================================================================
# 영상 업로드 및 관리 앱 설정
# ============================================================================

from django.apps import AppConfig


class VideosConfig(AppConfig):
    """
    영상 관리 앱 설정

    이 앱은 다음 기능을 제공합니다:
    - 영상 파일 업로드 (S3 또는 로컬)
    - 영상 메타데이터 관리 (길이, 해상도, FPS 등)
    - 영상 처리 상태 추적
    - 얼굴 데이터 관리
    """

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.videos'
    verbose_name = '영상 관리'
